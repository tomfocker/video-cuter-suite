#!/usr/bin/env bash

set -euo pipefail

source "$(dirname "$0")/common.sh"

require_local_repos
print_workspace_summary
print_info "Starting local source stack from video-cuter-suite..."
run_compose up --build -d
print_info "Open: http://127.0.0.1:18080"

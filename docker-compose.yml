services:
  funasr-server:
    image: ${FUNASR_IMAGE:-tomfocker/funasr-server:latest}
    pull_policy: always
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      CW_HOST: 0.0.0.0
      CW_PORT: 8000
      CW_DATA_DIR: /data
      CW_AUTO_DOWNLOAD_MODEL: "0"
      CW_VULKAN_ENABLE: "0"
      CW_DML_ENABLE: "0"
    volumes:
      - ./.docker-data/asr:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/healthz')\""]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 15s

  video-cuter-full:
    image: ${VIDEO_CUTER_FULL_IMAGE:-tomfocker/video-cuter-full:latest}
    pull_policy: always
    restart: unless-stopped
    expose:
      - "8000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8000/ >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 10s

  video-cuter-suite-gateway:
    image: ${GATEWAY_IMAGE:-tomfocker/video-cuter-suite-gateway:latest}
    pull_policy: always
    restart: unless-stopped
    environment:
      CUT_SUITE_ASR_UPSTREAM: ${CUT_SUITE_ASR_UPSTREAM:-funasr-server:8000}
      CUT_SUITE_FRONTEND_UPSTREAM: ${CUT_SUITE_FRONTEND_UPSTREAM:-video-cuter-full:8000}
    depends_on:
      funasr-server:
        condition: service_healthy
      video-cuter-full:
        condition: service_healthy
    ports:
      - "${CUT_HTTP_PORT:-18080}:8080"
      - "${FUNASR_HTTP_PORT:-18000}:18000"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/ >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 20
      start_period: 10s

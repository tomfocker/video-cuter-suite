services:
  funasr-server:
    build:
      context: ../funasr-server
      dockerfile: Dockerfile
    image: funasr-server:bundle-dev
    pull_policy: never

  video-cuter-full:
    build:
      context: ../video-cuter/full
      dockerfile: Dockerfile
    image: video-cuter-full:bundle-dev
    pull_policy: never

  video-cuter-suite-gateway:
    build:
      context: .
      dockerfile: Dockerfile
    image: video-cuter-suite-gateway:bundle-dev
    pull_policy: never
